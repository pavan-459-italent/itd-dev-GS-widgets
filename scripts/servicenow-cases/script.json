{
  "name": "servicenow-cases",
  "description": "Injects ServiceNow Case panel into topic detail page sidebar via Connectors SDK (no middleware)",
  "placement": "bodyEnd",
  "rules": [
    {
      "field": "pageType",
      "operator": "in",
      "value": ["topic"]
    }
  ]
}
