{
  "name": "servicenow-my-cases",
  "description": "Injects a community-wide 'My ServiceNow Cases' panel showing all Cases created by the currently logged-in user via Connectors SDK (no middleware). Loads globally (empty rules array); the script's own isTopicPage() check skips rendering on topic detail pages, where the separate servicenow-cases topic-linking panel already renders.",
  "placement": "bodyEnd",
  "rules": []
}
